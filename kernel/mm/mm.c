#include <mm.h>

// struct page_info page_dir[PAGE_NUM] __attribute__((aligned(PAGE_SIZE)));

int32 init_mm()
{
  // struct page_dir_info *p;
  // int i = 0;
  // for (; i < PAGE_NUM; i++)
  // {
  //   p = &pd_arr[i];
  //   p->flg = 0;
  // }
  return 0;
}
